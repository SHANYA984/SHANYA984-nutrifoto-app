# Frontend configuration (public values only)
VITE_API_BASE_URL=
VITE_VISION_PROVIDER=backend

# Backend-only secrets: configure these in the deployment platform, not here.
# VISION_PROVIDER_URL=
# VISION_PROVIDER_TOKEN=
